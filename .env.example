# Copy to `.env.local`. Values are baked in at build time on Railway.

NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Same secret as backend APP_TOKEN. You can use NEXT_PUBLIC_API_KEY instead (same value).
NEXT_PUBLIC_APP_TOKEN=choose-one-long-random-string
